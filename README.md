# DSA-Course-Lab
Labs of DSA course
Name: Qazi Muhammad Anas Alam
Registration Number: 553047

**Task 1:**
  In this task, I have to change an element of the numbers[] array and then print the updated array. Also i have to predict the output of the program.
  So first, I declare the size of array to 5 and also the numbers[] array of size 5. Then the number at index 2 that is third element of the array  is changed to 7. After it, using for loop, I printed the updated array in the console. So, the output should be:
2 4 7 8 10


**Task 2:**
  In this task, I have to declare an array and and add all of its elements in to a total variable which was first initialised with 0. And at the last print the total obtained.
    So first, I declare the size of the array to 5, numbers[] array and total variable to 0. Then I store the elements of the array taken by user in the array using for loop. In the second loop, I add them in the total variable and in the last I just print the total.
	Here, if we take number 1, 3, 5, 7, 9 from the user then the total should be 25
Enter exactly 5 numbers to add them:
1
3
5
7
9
Total = 25

  For the remaining cases that are given:
    1 2 3 4 5 
    Total = 15
    
    0 0 0 0 0
    Total = 0
    
    -2 4 -1 0 3
    Total = 4


**Task 3:**
    In this task, I have to use OOP concepts in C++. I have to create a class of Students and make 2 public attributes rollNumbers and marks. Then also create a void display functions to print the details of the students. In main class, I have to create 2 objects and give their details like rollNumber and marks and then use display function on them.
  Moreover, I have to change student 1 marks from 75 to 80 and then again call display function on both the students.

**Task 4:**
    In this task, I have to make an array of 8 integers and find the smallest and largest numbers and also their indices and print them. For this, I made a loop for both smallest and largest numbers and their indices. Their indices will store in smallestIndex and largestIndex variables.

**Task 5:**
  In this task, I have to use recursion function to reverse an array of 6 elements. I made a recursive function of reverseArray in which the base case is if start index is greater than or equal to end index, no need to reverse it but if it is not I will swap these values, then it gives a reversed array.

**Task 6:**
  In this task, I have to make an array of 10 elements and shift the distinct values to the left side of the array or move duplicate values to right side to get space for unique ones. Also I have to count those unique values and their order should not be changed.
